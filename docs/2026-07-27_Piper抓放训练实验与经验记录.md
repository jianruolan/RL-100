# 2026-07-27 Piper 单臂抓放训练实验与经验记录

## 1. 当日结论

今天得到的最好结果是：

```text
RL-100/data/outputs/
  piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch2000_seed42/
```

该权重已经能在真机上初步完成“接近物体—抓取—移动—放置”的完整行为。与当天较早的权重相比，它不再只在起点附近小幅振荡或几乎保持不动，说明这次数据清洗和训练采样调整是有效的。

目前仍存在两个明显问题：

- 轨迹较抖，动作连续性和平滑度不足；
- 成功率仍然偏低，对初始位姿、物体位置和观测噪声较敏感。

因此当前结果应视为“任务链路首次基本跑通”，还不能视为稳定部署版本。

补充实验 F 于 2026-07-27 22:31 启动、2026-07-28 00:35 完成，在实验 E
的相同设置上从头训练 4000 epoch / 40,000 update。该权重尚未做真机对照，
所以本节的“最好结果”仍指已经实际验证过的实验 E，而不是仅凭离线 loss 把实验 F
提前判为更好。

## 2. 最关键的经验

主要瓶颈不是任务复杂度或模型容量，而是原始数据中的监督冲突和每个 epoch 的有效训练量。

采集时，一条演示通常包含以下阶段：

1. 从标准起始姿态开始遥操作；
2. 抓取方块并放下；
3. 抬起机械臂；
4. 松开 Grip/deadman，随后一键回零。

原始 Zarr 把上述全过程连续写入同一条成功轨迹。`controlAccepted=False` 因而同时覆盖了等待、松开控制以及采集结束后的自动回零。对自主 BC 来说，这些帧不是期望策略的一部分，却会向模型提供“保持不动”或“返回零点”的监督。

这会导致几个问题：

- 标准起始姿态附近同时出现出发、等待和回零动作，形成一对多冲突；
- 轨迹末尾大量保持/复位动作在样本数上占优势；
- 删除无效控制帧后，如果仍把断点两侧拼成连续窗口，会制造不存在的高速跳变；
- 每条 episode 每 epoch 只抽 4 个窗口且 batch size 为 256 时，约 64 条训练轨迹只产生 256 个窗口，即每 epoch 只有 1 次参数更新。

最终有效方案同时处理了这四点：

- 仅保留 `controlAccepted=True` 且 `triggerHomeActive=False` 的任务控制帧；
- 对删帧和超过 120 ms 的时间间隔写入 `sequence_break`；
- DataLoader 禁止序列窗口跨越 `sequence_break`；
- 每条训练 episode 每 epoch 随机抽 10 个窗口，batch size 改为 64，使每 epoch 有 10 个 batch。

## 3. 数据集

### 3.1 原始增强数据集

路径：

```text
RL-100/data/piper_pick_and_place_augmented_chunk4.zarr
```

统计：

- 73 条成功轨迹；
- 22,083 帧 transition；
- RGB 为 `3 x 84 x 84`；
- 点云为 `512 x 3`，坐标系为 `d435i_depth_optical_frame`；
- 状态和动作均为 7 维：6 个关节量加 1 个夹爪量；
- 训练使用 `policy_action`，其夹爪维度是 Pico 开合命令锁存值，而不是夹爪反馈宽度。

该版本保留了 `controlAccepted=False`、等待和自动回零阶段，没有显式时间断点字段。

### 3.2 控制有效帧清洗数据集

路径：

```text
RL-100/data/piper_pick_and_place_augmented_chunk4_control_clean.zarr
```

来源为：

```text
RL-100/data/dataset/episode_*
```

转换报告：

```text
RL-100/data/piper_pick_and_place_augmented_chunk4_control_clean.conversion_report.json
```

处理规则：

- 从 78 条 rosbag 记录中按 metadata 接受 73 条成功轨迹，跳过 5 条明确标记为 failure 的轨迹；
- 仅保留 `controlAccepted=True`；
- 排除 `triggerHomeActive=True` 的自动回零帧；
- 相邻保留帧之间如有删帧，或者时间间隔超过 120 ms，则开始新 segment；
- 长度不足 2 帧的 segment 被删除；
- `sequence_break=True` 表示一个连续 segment 的首帧；
- 夹爪事件不会跨越断点传播；
- 训练窗口不能跨越断点，但可以从断点首帧开始。

清洗统计：

| 项目 | 数值 |
| --- | ---: |
| 清洗前同步帧 | 22,083 |
| 清洗后帧 | 15,643 |
| 保留源轨迹 | 73 |
| 跳过 failure 轨迹 | 5 |
| 删除的前缀帧 | 851 |
| 删除的后缀帧 | 4,532 |
| 中间被拒绝控制帧 | 352 |
| 短 segment 删除帧 | 705 |
| 原数据中的回零帧 | 2,448（与上述删帧有重叠，不能重复相加） |
| `sequence_break` 数 | 2,516 |
| 清洗后 `controlAccepted=False` | 0 |
| 清洗后 `triggerHomeActive=True` | 0 |

本次转换使用的等价命令为：

```bash
python3 tools/teleop_off2off_data/piper_rosbag_pick_place_to_rl100_zarr.py \
  --input RL-100/data/dataset \
  --output RL-100/data/piper_pick_and_place_augmented_chunk4_control_clean.zarr \
  --control-accepted-only \
  --max-retained-frame-gap-ms 120 \
  --overwrite
```

## 4. 当日训练实验

所有正式实验均使用 seed 42、纯 BC 阶段，公共时序设置为：

- `horizon=6`；
- `n_obs_steps=3`；
- `n_action_steps=4`；
- `chunk_as_single_action=True`；
- 训练/验证按 episode 划分，`val_ratio=0.125`，约 64 条训练轨迹、9 条验证轨迹；
- 验证集遍历全部合法窗口，不使用每 epoch 随机少量抽窗；
- 每个 epoch 的训练窗口按 `seed + epoch` 确定性、无放回地从每条轨迹中抽取。

### 4.1 实验总表

| 实验 | 数据 | 模型 | 抽样与 batch | epoch / update | 结果与用途 |
| --- | --- | --- | --- | --- | --- |
| A：VIB 初次启动 | 原始 22,083 帧 | DP3VIB + SkipNet + reconstruction + CM | 4 窗/episode，BS=256 | 启动后中止，epoch=0 | 无可比较结果，随后重新启动为 B |
| B：VIB 800 epoch | 原始 22,083 帧 | DP3VIB + SkipNet + reconstruction + CM | 4 窗/episode，约 256 窗/epoch，BS=256 | 800 epoch / 800 update | 能完成训练，但真机仍容易在起点附近小幅运动和振荡 |
| C：标准 DP3 800 epoch | 原始 22,083 帧 | 标准 DP3 + DDIM，无 VIB/reconstruction/CM | 4 窗/episode，BS=256 | 800 epoch / 800 update | loss 明显低于 VIB 版本，但真机效果仍不稳定 |
| D：标准 DP3 2000 epoch | 原始 22,083 帧 | 与 C 相同 | 4 窗/episode，BS=256 | 2000 epoch / 2000 update | 增加 epoch 后仍未根治起点附近停滞，说明仅延长训练不够 |
| E：清洗数据标准 DP3 | 清洗后 15,643 帧 | 与 C 相同 | 10 窗/episode，640 窗/epoch，BS=64 | 2000 epoch / 20,000 update | 当日最好；首次较明确实现抓取和放置，但仍抖且成功率不高 |
| F：清洗数据标准 DP3 长训练 | 与 E 相同 | 与 E 相同 | 与 E 相同 | 4000 epoch / 40,000 update | train loss 和 action MSE 更低，但 val 未优于 E；待做严格真机 A/B |

### 4.2 实验 A：中止的首次 800 epoch 启动

输出目录：

```text
RL-100/data/outputs/piper_pick_and_place_augmented_chunk4_episode4_epoch800_seed42
```

只保存了 epoch 0 / global step 0 的 checkpoint，不能用于比较收敛或真机效果。保留该目录是为了记录一次被重新启动替代的实验，不应误认为完成的 800 epoch 权重。

### 4.3 实验 B：DP3VIB/SkipNet 800 epoch

输出目录：

```text
RL-100/data/outputs/piper_pick_and_place_augmented_chunk4_episode4_epoch800_final_seed42
```

关键配置：

```text
encoder_type=dp3vib
model=skipnet
act=relu
use_vib=true
use_recon=true
use_cm=true
resolved scheduler_type=cm
windows_per_episode_per_epoch=4
batch_size=256
num_epochs=800
```

该实验完成 800 次 update。保留的 epoch 700 checkpoint 文件名中 `test_mean_score=-0.300`，但这里的 `test_mean_score` 只是 `-train_loss`，不是环境成功率，不能据此判断抓放成功。

### 4.4 实验 C：标准 DP3 800 epoch

输出目录：

```text
RL-100/data/outputs/piper_pick_and_place_augmented_chunk4_dp3_episode4_epoch800_seed42
```

相对实验 B 的模型消融：

```text
encoder_type: dp3vib -> dp3
model: skipnet -> dp3
use_vib: true -> false
use_recon: true -> false
use_cm: true -> false
scheduler_type: cm -> ddim
encoder_output_dim=64
diffusion_step_embed_dim=256
down_dims=[256, 512, 1024]
```

其余数据和抽样不变。完成 800 epoch / 800 update，epoch 700 保留 checkpoint 的 `test_mean_score` 约为 `-0.036`。这表明标准 DP3 更容易拟合该 BC 数据，但真机效果说明低训练 loss 本身仍不能消除数据标签冲突。

### 4.5 实验 D：标准 DP3 2000 epoch

输出目录：

```text
RL-100/data/outputs/piper_pick_and_place_augmented_chunk4_dp3_episode4_epoch2000_seed42
```

相对实验 C 只把训练从 800 epoch 增加到 2000 epoch，仍然是每条 episode 每 epoch 4 个窗口、BS=256，因此每 epoch 只有 1 次 update。

最终 W&B 指标：

| 指标 | 数值 |
| --- | ---: |
| train loss | 0.017174 |
| val loss | 0.018912 |
| train action MSE | 0.011830 |
| update 数 | 2,000 |

W&B：<https://wandb.ai/zyuwhatsoever-zhejiang-university/train_dp3%2Bpiper_pick_and_place_augmented/runs/w2g6n19o>

实验 D 说明，在同一份有冲突的数据上把 epoch 从 800 增到 2000，并不能单独解决“稍微移动后停滞/振荡”的问题。

### 4.6 实验 E：清洗数据 + 标准 DP3 + 20,000 update

输出目录：

```text
RL-100/data/outputs/piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch2000_seed42
```

相对实验 D 的调整：

```text
dataset:
  piper_pick_and_place_augmented_chunk4.zarr
  -> piper_pick_and_place_augmented_chunk4_control_clean.zarr

windows_per_episode_per_epoch: 4 -> 10
batch_size: 256 -> 64
updates_per_epoch: 1 -> 10
total_updates: 2,000 -> 20,000
```

模型仍是标准 DP3 + DDIM，没有改变计算精度，也没有加入 VIB、重建头或 CM。训练使用 10 步 DDIM 推理配置。

训练耗时约 57 分钟，最终 W&B 指标：

| 指标 | 数值 |
| --- | ---: |
| train loss | 0.008529 |
| val loss | 0.009998 |
| val diffusion loss | 0.009998 |
| train action MSE | 0.003372 |
| epoch | 2,000 |
| update 数 | 20,000 |

W&B：<https://wandb.ai/zyuwhatsoever-zhejiang-university/train_dp3%2Bpiper_pick_and_place_augmented/runs/il21lw1f>

注意：实验 D 和 E 的数据分布不同，因此 loss 不能作为严格的同数据消融指标。但结合真机现象，实验 E 在拟合指标和行为完整性上都明显更好。

### 4.7 实验 F：保持实验 E 设置，增加到 40,000 update

输出目录：

```text
RL-100/data/outputs/piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch4000_seed42
```

该实验的目的，是验证实验 E 在 2000 epoch / 20,000 update 时是否仍然欠拟合。
为了形成独立、可复现的 2000/4000 epoch 对照，本实验使用相同 seed 42 从头训练，
没有从实验 E 的 checkpoint 续训。这样也避免恢复一个学习率已经衰减到 0 的 optimizer/
scheduler 后再修改总 epoch 所带来的歧义。

相对实验 E，仅调整：

```text
num_epochs: 2000 -> 4000
total_updates: 20,000 -> 40,000
```

保持不变的关键项：

```text
dataset=piper_pick_and_place_augmented_chunk4_control_clean.zarr
encoder_type=dp3
model=dp3
scheduler_type=ddim
use_vib=false
use_recon=false
use_cm=false
windows_per_episode_per_epoch=10
batch_size=64
horizon=6
n_obs_steps=3
n_action_steps=4
seed=42
```

训练正常完成，进程 exit code 为 0，总耗时约 123.7 分钟。checkpoint 内部状态为：

```text
epoch=4000
global_step=40000
state_dicts=[ema_model, model, optimizer]
```

最终 W&B 指标：

| 指标 | 实验 E：2000 epoch | 实验 F：4000 epoch | 变化 |
| --- | ---: | ---: | ---: |
| train loss | 0.008529 | 0.005607 | 降低约 34.3% |
| 最终 val loss | 0.009998 | 0.011806 | 增加约 18.1% |
| train action MSE | 0.003372 | 0.002631 | 降低约 22.0% |
| 最终 learning rate | 0 | 0 | 相同 |
| update 数 | 20,000 | 40,000 | 2 倍 |

W&B：<https://wandb.ai/zyuwhatsoever-zhejiang-university/train_dp3%2Bpiper_pick_and_place_augmented/runs/kt9qi9o9>

不能只用最后一个随机 diffusion 验证点判断收敛，因此进一步比较完整验证曲线：

- 实验 E 的最佳 val loss 为 `0.009709`，出现在 epoch 1960；
- 实验 E 在 epoch 1800–1999 的 val loss 均值为 `0.011746`；
- 实验 F 的最佳 val loss 为 `0.010021`，出现在 epoch 3200；
- 实验 F 在 epoch 3000–3999 的 val loss 均值为 `0.012909`；
- 实验 F 后段 train loss 均值约 `0.006305`，明显低于其 val loss。

这些数据说明：4000 epoch 确实继续降低了训练误差和训练 action MSE，但没有提供
验证集泛化改善的证据，反而扩大了 train/val gap。就离线指标而言，“实验 E 明显欠拟合”
这一假设没有得到支持；实验 F 更接近继续拟合训练集，存在轻度过拟合迹象。

但 val diffusion loss 本身带有采样噪声，而且离线 loss 与抓放成功率不是同一指标。
最终仍需在相同起始姿态、物体位置、随机种子和测试次数下，对实验 E 与 F 做真机 A/B，
才能确定较低 action MSE 是否能换来更稳定的实际动作。

## 5. 代码与训练流程调整

今天为支持上述实验增加了三类能力。

### 5.1 按 episode 的每 epoch 随机抽窗

文件：

```text
RL-100/rl_100/dataset/adroit_dataset.py
RL-100/train.py
```

新增 `windows_per_episode_per_epoch` 和 `set_epoch(epoch)`：

- 每条训练 episode 独立抽取固定数量窗口；
- 同一 epoch 内无放回；
- 不同 epoch 使用不同但可复现的随机样本；
- 避免一个 epoch 必须遍历全部 transition；
- 验证集始终使用完整合法窗口。

这种定义下，必须同时记录 epoch 和 update 数。不同的 `windows_per_episode_per_epoch` 与 batch size 会使相同 epoch 数对应完全不同的优化步数。

### 5.2 连续性断点过滤

预处理在 Zarr 中写入 `sequence_break`，训练采样器删除跨越断点的窗口。此前 RL-100 的普通 SequenceSampler 没有按照真实时间 gap 自动删除窗口；这次过滤是显式加入的任务数据规则。

### 5.3 ETA 与配置可追溯性

训练每 10 epoch 输出：

```text
当前 epoch / 总 epoch
秒/epoch
预计剩余时间
预计总耗时
```

训练目录中的 `.hydra/config.yaml` 和 `.hydra/overrides.yaml` 是复现实验时的最终依据，不应只依赖 shell 命令或目录名。

## 6. 真机验证记录

最终权重使用以下核心条件测试：

```text
device=cuda:0
can=can_piper
rate=15 Hz
camera_fps=15 Hz
policy_subdir=bc
use_cm=false
DDIM steps=10
chunk_exec_steps=1
policy_replan_rate=15 Hz
```

当天保存了多次最终权重的执行日志：

```text
data/piper_inference/
  pick_place_control_clean_dp3_episode10_epoch2000_execute_01.json
  ...
  pick_place_control_clean_dp3_episode10_epoch2000_execute_14.json
```

日志本身没有人工标注的 `success/failure` 字段，因此不能从文件数量直接计算成功率。当前结论来自现场观察：可以初步完成抓取和放置，但抖动明显且成功率不高。

测试命令使用了三个 `--skip-*-check` 参数。这些仅适用于有人看护、急停可用的受控试验，不能作为长期运行配置。

## 7. 对结果的解释

最终实验同时改变了数据和优化步数，因此目前可以确认的是“组合方案有效”，还不能严格量化每项修改各自贡献多少。

较可信的解释是：

1. 删除回零、等待和未接受控制帧，减少了起始姿态附近的动作冲突；
2. 禁止跨断点采样，消除了伪造的瞬时大动作；
3. 20,000 次 update 提高了模型看到不同轨迹阶段的覆盖度；继续增加到 40,000
   update 会降低训练误差，但目前没有改善验证误差；
4. 标准 DP3 比当前 VIB/重建/CM 组合更适合这个规模的数据和纯 BC 目标；
5. 15 Hz 推理更接近数据约 13 Hz 的实际 transition 频率，时序分布偏移小于强行使用 20/30 Hz。

仍然抖动的可能来源包括：

- 每次只执行 action chunk 的第一步并重新规划，连续 DDIM 样本之间有随机差异；
- 数据本身的遥操作动作不够平滑；
- 点云和关节观测存在噪声；
- 夹爪开合附近是离散决策，容易放大轨迹差异；
- 数据覆盖的初始状态和物体位置仍然有限；
- `sequence_break` 很多，清洗后可用的长连续窗口数量可能不足。

## 8. 下一步建议

建议按以下顺序做可归因的实验，而不是同时改很多变量：

1. 对实验 E（2000 epoch）与 F（4000 epoch）做严格真机 A/B，每个权重至少使用
   相同条件重复若干次；
2. 固定实验 E 的训练配置，只比较原数据与 clean 数据，保持总 update 都为 20,000；
3. 固定 clean 数据，分别比较 4、10、20 窗/episode，并保持总 update 一致；
4. 为每次真机测试人工记录初始条件、是否抓到、是否成功放置、失败阶段和停止原因；
5. 对训练轨迹与推理动作计算一阶/二阶差分，量化速度和加速度抖动；
6. 在不改变模型精度的前提下，比较固定 diffusion noise、temporal action ensemble、执行 chunk 多步和底层限速/加速度规划；
7. 增补失败恢复、不同物体位置和标准起始姿态附近的高质量演示；
8. 最后再比较 VIB、CM 或更高推理频率，避免把训练问题与推理调度问题混在一起。

## 9. 复现入口

训练脚本：

```text
scripts/Diffusion/Offline/3D/train_policy_piper_pick_and_place_augmented_dp3.sh
```

推理脚本：

```text
tools/teleop_off2off_data/infer_piper_pick_and_place_policy.py
```

最终推理权重：

```text
RL-100/data/outputs/
  piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch2000_seed42/bc/
```

最终完整 checkpoint：

```text
RL-100/data/outputs/
  piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch2000_seed42/
  checkpoints/latest.ckpt
```

4000 epoch 待验证候选权重：

```text
RL-100/data/outputs/
  piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch4000_seed42/bc/
```

4000 epoch 完整 checkpoint：

```text
RL-100/data/outputs/
  piper_pick_and_place_augmented_chunk4_control_clean_dp3_episode10_bs64_epoch4000_seed42/
  checkpoints/latest.ckpt
```
